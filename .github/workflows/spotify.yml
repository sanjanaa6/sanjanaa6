name: Update Spotify status

on:
  schedule:
    - cron: '0 10 * * *'
  workflow_dispatch:

jobs:
  spotify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create placeholder Spotify card
        run: |
          mkdir -p output
          cat > output/spotify.svg <<'EOF'
          <svg xmlns="http://www.w3.org/2000/svg" width="500" height="120">
            <rect width="100%" height="100%" fill="#0d1117"/>
            <text x="20" y="50" fill="#58a6ff" font-family="Arial" font-size="20">Spotify integration</text>
            <text x="20" y="85" fill="#c9d1d9" font-family="Arial" font-size="16">Add your Spotify credentials to enable this workflow.</text>
          </svg>
          EOF

      - name: Commit Spotify output
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add output/spotify.svg
          git commit -m "Update Spotify card" || true
          git push
